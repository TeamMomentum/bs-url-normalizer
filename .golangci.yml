run:
  skip-dirs:

  skip-files:

linters:
  enable-all: true
  disable:
    - gochecknoglobals
    - gochecknoinits
    - lll
    - testpackage
    - varnamelen

issues:
  exclude-rules:
    - path: ".*_test.go$"
      linters:
        - scopelint
